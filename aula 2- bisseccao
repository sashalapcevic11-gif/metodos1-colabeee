#questao 1
import math
import numpy as np
import matplotlib.pyplot as plt

u= 142.7376
n=6
eppara=0.5*10**(2-n)
epest=100

def f(m):
  g=9.81
  c=0.25
  t=4
  v=36
  return np.sqrt(g*m/c)*np.tanh(np.sqrt(g*c/m)*t)-v

m=np.linspace(140,150,200)

for i in range(0,199):
  if f(m[i])*f(m[i+1])<0:
    mu = m[i+1]
    ml = m[i]

    print(mu)
    print(ml)

    if epest>eppara:
      mr = (mu+ml)/2

      if f(mr)*f(mu)<0:
        ml=mr
      else:
        mu=mr

      epest=abs((mr-mu)/mu)*100

    print(mr)


#questao 2   

import math
import matplotlib.pyplot as plt
import numpy as np

def f(x):
  return (x**2)-2

x=np.linspace(-2,2,20)

y=f(x)

plt.plot(x,y,'--or',label='raizes')
plt.legend()
plt.xlabel('valores de x')
plt.ylabel('valores de y')
plt.grid()
plt.show()

import math
import matplotlib.pyplot as plt
import numpy as np

def f(x):
  return (x**2)-2

x=np.linspace(-2,2,20)

for i in range(0,19):

  if f(x[i])*f(x[i+1])<0:
    xu=x[i+1]
    xl=x[i]
  
    eppest=100
    n=6
    eppara= 0.5*10**(2-n)
    xr=(xu+xl)/2

    while eppest>eppara:
      xold=xr

      if f(xr)*f(xu)<0:
        xl=xr

      else:
        xu=xr

      xr=(xu+xl)/2

      eppest=abs((xr-xold)/xr)*100
      
    print(xr)
    


